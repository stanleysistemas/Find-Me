using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;

using System.Net;
using System.Threading.Tasks;
using static Newtonsoft.Json.JsonConvert;

namespace Findme.Services
{
  
    public class WebApiService
    {
        //const string webapi = "https://aspnetidentitywebapi20161025090604.azurewebsites.net/";
        //const string acaoregistro = "api/account/create";

        //public async Task<CriaRegistro> PostRegistro()
        //{
        //    using (var client = new HttpClient())
        //    {
        //        var url = string.Format(webapi, id, units.ToString().ToLower());
        //        var json = await client.GetStringAsync(url);

        //        if (string.IsNullOrWhiteSpace(json))
        //            return null;

        //        return DeserializeObject<WeatherForecastRoot>(json);
        //    }

        //}

       // static HttpClient client = new HttpClient();

    }

    

}